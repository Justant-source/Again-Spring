# 시각 정본

> **28화면 시각 정본**: [`frontend/design/다시봄 광장형 UX (standalone).html`](../../../design/다시봄%20광장형%20UX%20(standalone).html)  
> Claude Design 호출 시 "이 HTML의 톤으로 만들어줘"로 제공.

---

## 화면 목록

standalone HTML 내 30개 C3_* 컴포넌트 · 28화면 (+서브) — 상세는 [components.md](../components.md) §28화면 인덱스 참조.

---

## PNG 캡처 (개별 화면 저장소)

현재 이 디렉토리에 PNG 파일이 추가되면 아래 표에 등재:

| 캡처 파일 | 화면 | 톤 | 캡처 날짜 |
|---|---|---|---|
| (비어있음 — standalone HTML이 시각 정본으로 대체) | — | — | — |

---

## 갱신 정책

화면 디자인이 **크게 변경**될 때 standalone HTML 교체 (`.request/` 에 새 HTML 수령 후 `frontend/design/`로 배치).

캡처 PNG 추가 시:
1. dev 서버(`npm run dev`) 실행
2. 해당 화면 → 브라우저 DevTools 모바일 뷰 (360px×780px 기준)
3. 스크린샷 저장 (PNG) → 이 폴더에 추가 + 위 표 갱신

*최종 갱신: 2026-06-03*
