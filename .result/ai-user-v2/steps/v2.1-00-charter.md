# v2.1 Phase 0 — 창립 & 동결 work-order 기록

> 세션: 2026-06-21 | 실행자: Claude Code (Opus 플래너 → Sonnet 실행)

---

## 완료 체크리스트

- [x] `roadmap.md`: v2 CLOSED 헤더 + v2.1 전체 섹션 추가
- [x] `charter-v2.1.md`: 재구성 3명제·규율·코드훅·Phase 요약 창립
- [x] `decisions.md`: V2-D04(재구성·eval 재정립) + V2-D05(kill criterion 사전 등록) 추가
- [x] `STATE.md`: v2.1 라이브 포인터 초기화, v2 CLOSED 표기
- [x] `steps/v2.1-00-charter.md`: 이 파일 (work-order 기록)
- [x] `npm run lint:docs`: exit 0 (경고 6건 기존 날짜스탬프, 새 파일 관련 없음)
- [🔴] **kill criterion 오너 확정**: 제안 ≤60% naive ≥3인 — 오너 명시 확정 대기. 측정(Phase 5) 전 필수.
- [ ] git commit + push

---

## 재구성 3명제 동결 요약

| 명제 | 동결 내용 |
|---|---|
| A | 오너 = 게이트 은퇴 → 캘리브레이션 전용. 게이트 = 신선 캐주얼 ≥3인 |
| B | 카테고리 정렬 = 제품 적합성(다시봄 6 광장 피드 일관). 회피 = 퇴행 금지 제약만 |
| C | 타깃 taxonomy = `PostCategory` 6개(`COUPLE·MARRIED·FRIEND·FAMILY·WORK·OTHER`)만 |

---

## 핵심 발견 (탐사 확정)

- `example_bank.category` 전량 `'talk'` → 광장 RAG **현재 비활성**. Phase 2 라벨 덮어쓰기로 즉시 해결(검색 코드 0변경).
- 카테고리 = 페르소나 고정(`topCategory` argmax). 코드 변경 0, config 재배분만.
- 분류기 재사용: `EmbeddingService.embed_batch`(GPU) + `topic_synthesizer._synthesize_with_llm`(LLM-classify 템플릿) — Claude 토큰 ≈0.

---

## Phase 0 블로커 현황

**🔴 kill criterion 오너 확정**: 제안 "≤60% naive ≥3인" 사전 등록(V2-D05). Phase 5 측정 전 오너 명시 확정 필요. Phase 1(설계)·Phase 2(분류)는 확정 전 진행 가능.
