⚠️ **DRAFT — Phase 5 named-tell 확인 후 적용**

# AI-User v2.1 Phase 6 "결정론 다양화"

> **차수**: Phase 6 (DRAFT)  
> **작성**: 2026-06-21  
> **상태**: Phase 5 blind_kit 평가자 feedback (named-tell) 대기 중  
> **목표**: 종결 유형 & 감정 표현의 결정론적 다양화

---

## 문제정의

### Phase 5b 상황

블라인드 평가 결과 (`blind_kit_v3_key.md`):
- **PASS**: 봇 식별률 55.6% (≤60% threshold 충족)
- **남은 미탐(tell)**: 5개 포스트 (P3, P5, P13, P15 + 커뮤니티 맥락)

### 탐지된 v2 tells (Phase 5b 종합)

| Tell 유형 | 예시 | 현황 |
|---|---|---|
| **이중질문 종결** | "이게 좋은 건지 나쁜 건지 모르겠네요" | P1에서 식별됨 (5/9 중 1) |
| **감정 평탄화** | "바보같음·찐따같음" 표현은 있으나 느낌표·감정 수식어 부재 | P7에서 식별됨 (5/9 중 1) |
| **"~건지 모르겠" 수렴** | "하는 건지 모르겠네요", "하는지 모르겠어요" | P5/P7/P9/P13 미탐지 (4개) |
| **어미 변형 미필터** | "건지→는지→음" 어미 변동 | 미등록 (표면형 추격 금지) |

### Phase 6 핵심

**whack-a-mole 금지**: "~건지 모르겠음" 단일 어미 변형을 추격하지 않는다.  
**대신**: 종결 유형 자체를 분산 → 1회 라운드 한정.

- 감정 폭발형 (느낌표·강한 감정)
- 미완형 (혼란·무기력)
- 자문형 (상대에게 묻기)
- 발산형 (포기하는 톤)

→ 이중질문 종결("~인지 ~건지") 지양

---

## 코드 리뷰 결과

### 1. PromptAssembler 구조

**파일**: `/home/justant/Data/Again-Spring/ai-user/llm/src/main/java/com/againspring/aiuser/llm/service/PromptAssembler.java`

**현황**:
- 생성 지침: `buildSystem()` (line 491–593) → 이미 "핵심 4가지" 규칙 있음
  - 구체 사건 필수 (trigger)
  - 배경 축소
  - 감정 토로 강화
  - **미완성감 유지** (line 551–552)

- 다양성 시드: `VARIETY_SEEDS[]` (line 89–104) → 50% 확률로 랜덤 주입
  - 종결 다양화 지침 **없음**
  - 현재: 구체 사건, 1인칭, 해결책 금지 등만 있음

**삽입 위치**: 
- **Option A**: `VARIETY_SEEDS[]` 배열에 3~4개 종결 다양화 지침 추가
- **Option B**: `buildSystem()` 반환 문자열 말미에 별도 섹션 추가

### 2. OutputSanitizer 구조

**파일**: `/home/justant/Data/Again-Spring/ai-user/llm/src/main/java/com/againspring/aiuser/llm/service/OutputSanitizer.java`

**현황**:
- 감정 표현 강화 로직 **없음** (감정 평탄화 대응 불가)
- THEQOO 클린업만 특화 (line 203–224)
- 오타 주입 (R9 Track A) → 이미 자연스러운 오류 패턴 있음

**후처리 가능성**:
- 느낌표 0개 포스트 감지 → 선택적 감정 강화? (복잡도 높음)
- OR: 프롬프트 → OutputSanitizer 전달 선택성 제어

→ **OutputSanitizer는 건드리지 않음** (감정 추가는 생성 단계 몫)

### 3. SelfCritiqueService 구조

**파일**: `/home/justant/Data/Again-Spring/ai-user/llm/src/main/java/com/againspring/aiuser/llm/service/SelfCritiqueService.java`

**현황**:
- `quickCheck()` (line 72–203): 결정론적 체크 7가지 (7점 만점)
  - 온점 사용 (-2점)
  - 쌍따옴표 (-2점)
  - 반복 마무리 질문 (-1점)
  - **감정 추상명사 직접 서술** (line 107–110, -1점): "서운함", "답답함" 등
  - AI 상투구 (-2점)
  - 강조어 남발 (-1점)
  - ㅠ 남발 (-1점)

- `extraCliches` (line 36, 275–288): runtime 추가 패턴 + 프로퍼티 동적 컴파일
  - 현재: 클리셰 5종 등록 가능

**감정 평탄화 감지**:
- 느낌표 0개 감지 로직 없음
- "감정 표현 부재" 패턴 미구현

---

## 구체 변경안

### 1. PromptAssembler — 종결 다양화 지침 추가

**위치**: `buildSystem()` 반환값 말미 (line 573 `<<<PERSONA_SECTION>>>` 이후)

**변경 전**:
```java
## 페르소나 특성
%s
%s%s%s""".formatted(...);
```

**변경 후** (PSEUDOCODE):
```java
## 페르소나 특성
%s

## 📌 종결 다양화 (이번 글만 적용)
생성할 글의 마무리 종결을 다음 중 **1가지만** 선택해 적용하세요 (매번 다르게):

### 1. 감정 폭발형
강한 감정·느낌표 우선. 상황 묘사 후 감정으로 끝내기.
예: "진짜 화가 난다... 이대로는 못 산다!"

### 2. 미완형 (혼란·무기력)
"어떻게 해야 할지 모르겠어" 유형. 단, 이중질문("~인지 ~건지") 금지.
예: "그래서 지금 나는 뭘 해야 하는 건지... 정말 모르겠어"

### 3. 자문형 (상대에게 묻기)
상대방이나 독자에게 직접 질문. 조언·판정 요청.
예: "내가 잘못한 걸까? 솔직히 내 입장에 공감이 돼?"

### 4. 발산형 (포기·한숨)
마지막에 힘을 빼거나 자조. "그냥", "아 모르겠다" 톤.
예: "아무튼 그냥... 뭔가 피곤하네"

⚠️ **금지**: "~인지 ~건지 모르겠는데" 종결 (이중질문) 지양

%s
%s%s%s""".formatted(...);
```

**OR 더 간단하게** (VARIETY_SEEDS 활용):
```java
private static final String[] VARIETY_SEEDS = {
    // ... 기존 12개 ...
    "[종결 다양화] 이번 글은 강한 감정으로 끝내기 — 느낌표, 분노, 절망으로 폭발하는 톤",
    "[종결 다양화] 이번 글은 혼란으로 끝내기 — '어떻게 해야 할지 모르겠어' (단, 이중질문 금지)",
    "[종결 다양화] 이번 글은 상대에게 묻기 — '너는 어떻게 생각해?' 스타일로 끝내기",
    "[종결 다양화] 이번 글은 포기·한숨으로 끝내기 — '그냥', '아 모르겠다', '피곤하네' 톤",
};
```

### 2. PromptAssembler — 감정 표현 강화 지침

**현황**: R7 "구어체 강화" (line 578) 이후 추가 지침 없음

**변경**: `buildSystem()` "슬랭 수준" 섹션 앞에 삽입:
```java
## 감정 표현 다양화
감정을 구체적 행동·감각으로 표현. 추상명사(서운함·답답함·분노) 직접 사용 금지.

❌ "정말 서운했어" → ✅ "눈물이 났어", "자꾸 한숨만 나왔어"
❌ "답답함을 느꼈어" → ✅ "목이 메었어", "가슴이 철렁했어"

느낌표 활용:
- 강한 감정 글: 느낌표 3~5개 자연스럽게
- 혼란·무기력 글: 느낌표 0~1개 (차분하게)
- 자문형 글: 물음표 주로 (답답함 없이)
```

### 3. SelfCritiqueService — 감정 평탄화 감지

**위치**: `quickCheck()` 메서드 (line 80–203)

**추가 체크** (수도코드):
```java
// 12. 감정 표현 부재 — 느낌표 0개 + 감정 수식어 0개
int exclamationCount = countMatches(Pattern.compile("[!！]"), text);
int emotionWords = countMatches(EMOTION_WORD_PATTERN, text);  // "힘들어", "화났어", "울컥했어" 등
if (exclamationCount == 0 && emotionWords < 2) {
    score -= 1;
    issues.add("감정 표현 부재 — 구체적 감정 행동 추가");
}
```

**EMOTION_WORD_PATTERN** (신규):
```java
private static final Pattern EMOTION_WORD = Pattern.compile(
    "(?:힘들|화[났내]|울컥|눈물|짜증|분노|절망|이겨|쓸쓸|허탈|억울|" +
    "쎄하|낌새|불현|끓어오르|목이 메|가슴이 철렁|답답|무섭|미안|" +
    "꺼려|거부감|불안|한숨|한심|자존|자학|죄책)");
```

---

## Phase 5 → Phase 6 매핑표

### If Phase 5 named-tell = "이중질문 종결만 감지"

→ **Action: 이중질문 종결("~인지 ~건지") 명시 금지 지침 추가**
  - PromptAssembler: `buildSystem()` "종결 다양화" 섹션에 명시
  - SelfCritiqueService: 이중질문 패턴 감지 추가 (score -2)

### If Phase 5 named-tell = "감정 평탄화 톱 reach"

→ **Action: 감정 표현 강화 지침 + quickCheck 감지 로직 추가**
  - PromptAssembler: "감정 표현 다양화" 섹션 추가
  - SelfCritiqueService: "감정 표현 부재" 체크 추가

### If Phase 5 named-tell = "어미 수렴만 지속" (whack-a-mole)

→ **Action: 종결 유형 분산만** (어미 변형 추격 금지)
  - PromptAssembler: VARIETY_SEEDS 또는 buildSystem에 "4가지 종결 유형" 지침
  - OutputSanitizer: 변경 없음 (후처리로 표면형 추격 금지)
  - SelfCritiqueService: "~건지 모르겠" 정규식 확장 금지

### If Phase 5 named-tell = "3가지 혼합"

→ **Action: 위 3가지 모두 적용** (1회 라운드 한정)
  - PromptAssembler: 종결 다양화 + 감정 표현 강화 모두
  - SelfCritiqueService: 이중질문 + 감정 평탄화 모두 감지

---

## 구현 체크리스트

### Phase 5 평가자 feedback 대기 중 (✋ BLOCKED)

1. **named-tell 확인**: 
   - [ ] Phase 5b 평가 완료 및 tell 패턴 명시 (예: `kit-v2.1-phase5-ANSWER-KEY.md`)
   - [ ] 패턴 분류 (이중질문 / 감정 평탄화 / 어미 수렴 / 혼합)

2. **코드 변경** (Phase 5 tell 확인 후):
   - [ ] PromptAssembler.java: `buildSystem()` 수정 (종결 다양화 ± 감정 표현 강화)
   - [ ] SelfCritiqueService.java: `quickCheck()` 신규 체크 추가
   - [ ] `extraCliches` 프로퍼티 업데이트 (이중질문 패턴 추가, if needed)

3. **테스트**:
   - [ ] 신규 지침이 LLM에 적용되었는지 확인 (프롬프트 조립 로그)
   - [ ] quickCheck 신규 체크가 trigger되는지 (unittest)
   - [ ] CASUAL 포스트 생성 샘플 검증 (종결 다양화 체크)

4. **dev 배포**:
   - [ ] `cd backend && ./gradlew bootRun` + LLM 워커 확인
   - [ ] E2E 스모크 테스트 (별도 spec: `tests/e2e-realbe/journeys/ai-user-v2-1-diversification.spec.ts`)

---

## 토큰 & 성능 영향

### PromptAssembler 영향

**추가 텍스트량**:
- VARIETY_SEEDS 신규 4개: ~300 토큰 (user 프롬프트 끝, 50% 샘플)
- buildSystem "종결 다양화" 섹션: ~400 토큰 (system prefix, 캐시 가능)
- 감정 표현 지침: ~150 토큰

**합계**: +400 토큰 (캐시 hit시 무비용)

### SelfCritiqueService 영향

**추가 검사**:
- EMOTION_WORD 정규식 매칭: O(n) where n = 텍스트 길이
- 영향: FAIL score 추가 감지 → 재생성 호출 확률 증가 (토큰 사용 증가)
- 추정: quickCheck FAIL 5~10% 증가 → 재생성 LLM 호출 5~10% 증가

---

## 주의사항 (v2 교훈)

1. **단일 tell 추격 금지** — "~건지 모르겠음" 어미 변형을 정규식으로 확장하지 말 것
   - 대신: "종결 유형 4가지" 분산 (음 수열 무시)

2. **R7 준수** — CASUAL 확률 25%, `computeCasualProb` 불변
   - 느낌표 강제 주입 금지 (자연스러움 해침)

3. **캐싱 보호** — PromptAssembler 변경 시 `<<<PERSONA_SECTION>>>` 마커 위치 확인
   - 정적 prefix (캐시) 변경 금지
   - 가변 내용만 마커 아래로

4. **감정 지침은 제안일 뿐** — LLM이 무시할 수 있음
   - OutputSanitizer 후처리가 최후 방어
   - BUT: 감정 추가는 후처리로 불가 (생성 단계 몫)

5. **Phase 6 = 1회 라운드** — 이 변경이 효과 없으면 다음 phase로 이동
   - Whack-a-mole 무한루프 금지 (charter v2.1 명제 B)

---

## 최종 파일 경로 및 권위본

### 코드 변경
- PromptAssembler: `/home/justant/Data/Again-Spring/ai-user/llm/src/main/java/com/againspring/aiuser/llm/service/PromptAssembler.java` (line 491–593)
- SelfCritiqueService: `/home/justant/Data/Again-Spring/ai-user/llm/src/main/java/com/againspring/aiuser/llm/service/SelfCritiqueService.java` (line 80–203)

### 참고 문서
- 현황: `blind_kit_v3_key.md` (tell 분석)
- Charter: `charter-v2.1.md` (whack-a-mole 금지 명제 B)
- Phase 5 결과: `.result/ai-user-v2/eval/v2.1/phase5/kit-v2.1-phase5-ANSWER-KEY.md` (예상 위치, TBD)

---

**상태**: ⏸️ **DRAFT — Phase 5 평가자 feedback 대기**  
**다음 단계**: Phase 5 named-tell 확인 → 해당 action 선택 → 코드 수정 → dev 테스트 → prod 배포

