# v2.1 Phase 8 FAIL Contingency — 옵션 설계 문서

> **세션**: 2026-06-22 | Phase 8 FAIL(>60%) 대비 설계  
> **목적**: 코드 변경 없이 설계만 작성. Phase 8 결과 확인 후 실행 기준 정의.

---

## Executive Summary

Phase 8 FAIL(식별률 >60%)의 3가지 옵션 설계:

| 옵션 | 대상 | 비용 | 효과 | 위험 | 권고 |
|---|---|---|---|---|---|
| **(a) QLoRA 임계 재평가** | 조건3(광장별 clean count) | 낮음(설정변경) | 중간(ML트레이닝) | 코퍼스 부족 가능 | FAIL 80%+인 경우 |
| **(b) Phase 6 추가 라운드** | T3(광장 불일치) 집중 | 낮음(프롬프트) | 높음(생성 직접) | 캐싱 깨짐 우려 | **FAIL 60~75% (최우선)** |
| **(c) 광장 보강** | FRIEND/WORK thin plaza | 높음(콘텐츠 수작업) | 낮음(데이터 량 한계) | NATEPAN 구조 한계 | 보조 옵션 |

---

## 배경 정리

### Phase 5 FAIL (식별률 80%)

**4개 named-tell 파악**:
- T1: 동일사건-반복변주 (4/5 AI 계정)
- T2: 미완-종결 (문제 5)
- T3: 광장-내용-불일치 (문제 3·5 강함)
- T4: 종결-단조화 (문제 3)

### Phase 6 개입 (식별률 → ?)

| Tell | 조치 | 코드위치 | 생성일 |
|---|---|---|---|
| T1 | recentOutputsBlock 강화 | PromptAssembler.java:180 | 2026-06-21 |
| T2 | 미적용 (T4 가이드로 간접 커버) | — | — |
| T3 | **CATEGORY_GUIDE** 추가 | PromptAssembler.java:111-117 | 2026-06-21 |
| T4 | VARIETY_SEEDS 종결 4종 추가 | PromptAssembler.java:104-108 | 2026-06-21 |

### Phase 7 QLoRA 판정

**조건 3 미충족 → 비발동**:
- FRIEND 165 < 임계
- WORK 156 < 임계
- NATEPAN 랭킹 섹션 구조상 보강 불가

**의문**: Phase 6 적용 후 FAIL 원인이 T3인지, 아니면 T3 가이드 표현이 충분하지 않은지 불명.

---

## 옵션 상세 설계

### 옵션 (a): QLoRA 데이터게이트 임계 재평가

#### 목표
조건 3을 충족시켜 QLoRA 파인튜닝 활성화 → ML 학습으로 나머지 tell 흡수.

#### 근거
- Phase 5에서 T1·T4는 프롬프트만으로 상당 부분 해소 (Phase 6 sanity 71% 성공)
- T3는 여전히 경계선 케이스 2건 (전남친→COUPLE 오판 등)
- QLoRA가 미세 tell(경계선 케이스, 고급 문체 일관성)까지 학습할 수 있다면, 임계 재조정으로 활성화 가능

#### 설계: 조건 3 임계 동적 조정

**현황**:
```
조건 3: 광장별 clean ≥ 임계 (오너 미확정, 추정 "수백")
```

**제안 A (보수)**: 임계 = 100건
```
COUPLE  358 ✅ (100 이상)
MARRIED 539 ✅
FRIEND  165 ✅ (新 충족)
FAMILY  488 ✅
WORK    156 ✅ (新 충족)
OTHER   1,590 (제약 없음)
```
→ 조건 3 충족 → Phase 7 재판정 → QLoRA 활성화

**제안 B (중도)**: 임계 = 150건
```
COUPLE  358 ✅
MARRIED 539 ✅
FRIEND  165 ✅ (보유 여유 15)
FAMILY  488 ✅
WORK    156 ✅ (보유 여유 6)
OTHER   1,590
```
→ WORK 마진 매우 좁음. 테스트 후 삭제되면 재평가 필요

**제안 C (진취)**: 임계 = 200건
```
COUPLE  358 ✅
MARRIED 539 ✅
FRIEND  165 ❌ (부족 35)
FAMILY  488 ✅
WORK    156 ❌ (부족 44)
OTHER   1,590
```
→ FRIEND·WORK 미충족. 추가 보강 필요 (옵션 c 병행)

#### 코드 위치 (읽기만)

**파일**: `ai-user/learning/app/services/persona_strengthener.py`

```python
# SFT 데이터 빌더 입력 — 광장별 clean count
# 코드에는 현재 하드코딩 없음. DB 쿼리 결과로 판정:
#
#   SELECT post.category, COUNT(*) as clean_count
#   FROM ai_user_post post
#   WHERE is_clean = true AND created_at >= '2026-06-21'
#   GROUP BY post.category;
#
# (실제 쿼리 위치: orchestrator 또는 learning API 엔드포인트)
```

#### 실행 절차

1. **오너 결정**: 임계를 A·B·C 중 선택 (또는 커스텀)
2. **DB 확인**: 각 광장의 clean count 재계산
3. **Phase 7 재판정**: 조건 1·2 재확인 (변경 없음)
4. **QLoRA SFT 데이터 빌더 트리거**:
   - WSL에서 `python ai-user/learning/.../ build_sft_dataset.py --gateway-threshold=<임계값>`
   - example_bank 광장별 필터링
5. **QLoRA 트레이닝**: RTX 3090 활용, 약 4~6시간
6. **Phase 8 재평가**: 신선 평가자 3인 + QLoRA 모델

#### 부작용 검토

| 항목 | 위험도 | 완화 |
|---|---|---|
| 코퍼스 품질 저하 (임계 낮음) | 중간 | threshold=150 에서 테스트; WORK 여유 6만으로는 불안 |
| 트레이닝 오버피팅 | 낮음 | SFT 데이터 ~500건 수준은 안전. 단, LoRA rank/lora_alpha 조정 필수 |
| 캐싱 영향 | 없음 | QLoRA는 LLM 모델 내 매개변수만 변경, 프롬프트 불변 |
| 배포 시간 | 낮음 | 모델 교체만. BE 빌드 필요 없음 (remote LLM 갱신) |

**결론**: **임계 A(100건) 권고**. WORK 여유 충분, 신중하되 가능성 높음.

---

### 옵션 (b): Phase 6 추가 라운드 — T3 광장 강화 (최우선)

#### 목표
Phase 6에서 적용한 CATEGORY_GUIDE가 "1줄 가이드"라는 한계 극복.  
T3(광장 불일치) 케이스 중 경계선(전남친→COUPLE, 여행자금→COUPLE 등)을 명시적으로 배제.

#### 근거
- Phase 6 sanity: T3 71% 성공 → **29% 여전히 불일치**
- 기존 CATEGORY_GUIDE는 "갈등만" 범주화 (예: "연인 관계 갈등만")
- 경계선 사례(가족이 개입한 연인 갈등, 돈 문제 혼합 갈등)에 대한 추가 세부 지침 필요

#### 설계: CATEGORY_GUIDE 세부화

**현재 (Phase 6)**:
```java
private static final java.util.Map<String, String> CATEGORY_GUIDE = java.util.Map.of(
    "COUPLE",  "연인 관계(남친·여친·전남친·전여친) 갈등만 작성. 가족·직장·친구 갈등은 이 광장에 맞지 않음.",
    "MARRIED", "부부·배우자·시댁·처가 갈등만. 미혼 연인 이야기는 제외.",
    "FRIEND",  "친구 관계 갈등만. 연인·가족·직장 갈등은 이 광장에 맞지 않음.",
    "FAMILY",  "가족(부모·형제자매·친척) 갈등만. 연인·직장·친구 이야기는 제외.",
    "WORK",    "직장·업무·상사·동료 갈등만. 연인·가족·친구 갈등은 이 광장에 맞지 않음.",
    "OTHER",   ""
);
```

**제안 (세부화)**:
```java
private static final java.util.Map<String, String> CATEGORY_GUIDE = java.util.Map.of(
    // COUPLE: 미혼 2인 사이 갈등만. 가족이 개입하거나 돈·주거 문제가 중심이면 제외.
    "COUPLE",  "미혼 연인(남친·여친·전남친·전여친) 2인 사이의 갈등만. " +
               "⚠️ 제외: 가족이 개입한 갈등(부모·형제 연루), 돈·주거·결혼 시기 협상 문제는 FAMILY/OTHER, " +
               "직장 영향 받는 연인 싸움은 WORK으로 분류할 것.",
    
    // MARRIED: 부부 사이 또는 시댁·처가와의 갈등만.
    "MARRIED", "부부·배우자·시댁·처가 관계 갈등만. " +
               "⚠️ 제외: 미혼 연인 이야기, 시댁과의 금전 분쟁(FAMILY로 분류 권장), " +
               "부부 중 하나의 직장 문제가 갈등 중심이면 WORK으로.",
    
    // FRIEND: 친구 관계 갈등만. 지인이 추가되거나 연인 요소 있으면 제외.
    "FRIEND",  "친구·지인 관계 갈등만. " +
               "⚠️ 제외: 친구 중 한 명이 상대의 연인/배우자와 문제(연인 개입), " +
               "친구 관계에 가족이 개입(형제·부모)하면 FAMILY, " +
               "직장 동료 문제는 WORK으로.",
    
    // FAMILY: 부모·형제자매·친척과의 갈등, 또는 가족 금전 문제.
    "FAMILY",  "부모·형제자매·친척과의 갈등, 상속·생활비 문제. " +
               "⚠️ 제외: 배우자와의 갈등(MARRIED), " +
               "친구가 중심 인물(FRIEND), 직장 문제(WORK).",
    
    // WORK: 직장·업무·상사·동료 갈등만.
    "WORK",    "직장·업무·상사·동료 갈등만. " +
               "⚠️ 제외: 직장 인간관계가 있어도 애초 갈등이 연인/부부/가족 주제면 그 광장으로(예: 직장동료와 연인 사이 문제→COUPLE/MARRIED), " +
               "퇴사/전직 관련 배우자 의견 충돌은 MARRIED으로.",
    
    "OTHER",   ""
);
```

#### 코드 적용 위치

**파일**: `ai-user/llm/src/main/java/com/againspring/aiuser/llm/service/PromptAssembler.java`

**라인 111-117** (현재):
```java
private static final java.util.Map<String, String> CATEGORY_GUIDE = java.util.Map.of(...);
```

**라인 152-153** (문자열 삽입 위치):
```java
String catGuide = CATEGORY_GUIDE.getOrDefault(req.getCategory() != null ? req.getCategory() : "OTHER", "");
// 라인 174에서 프롬프트에 주입:
catGuide.isBlank() ? "" : " (⚠️ " + catGuide + ")"
```

#### 샘플 프롬프트 변경 (Before/After)

**Before (Phase 6)**:
```
카테고리: COUPLE (⚠️ 연인 관계(남친·여친·전남친·전여친) 갈등만 작성. 가족·직장·친구 갈등은 이 광장에 맞지 않음.)
```

**After (Phase 6 추가)**:
```
카테고리: COUPLE (⚠️ 미혼 연인(남친·여친·전남친·전여친) 2인 사이의 갈등만. 
⚠️ 제외: 가족이 개입한 갈등(부모·형제 연루), 돈·주거·결혼 시기 협상 문제는 FAMILY/OTHER, 
직장 영향 받는 연인 싸움은 WORK으로 분류할 것.)
```

#### 부작용 검토

| 항목 | 위험도 | 완화 |
|---|---|---|
| 캐싱 깨짐 | **높음** | CATEGORY_GUIDE 재작성 시 4096토큰 prefix 변경 → 1시간 TTL 동안 misses |
| 프롬프트 길이 증가 | 낮음 | 카테고리 가이드만 +~200자. system(4096토큰) 대비 무시할 수준 |
| LLM 해석 과부하 | 낮음 | 정보량 증가 아님, 구조화만 강화 (LoRA prefix caching 적용 LLM은 무시) |
| 의도 역효과 | 중간 | 너무 자세한 "제외" 리스트가 LLM 혼동 → 리스트 대신 "핵심 1줄 + 반례 1개" 구조 권장 |

**완화 전략**:

1. **캐싱 타이밍**:
   - 새벽 4시(트래픽 최소) 배포
   - 또는 dev에서 먼저 1일 테스트 후 prod로

2. **프롬프트 구조 개선**:
   ```java
   // 제안: "제외 리스트"보다 "핵심 1줄"로 단순화
   "COUPLE",  "미혼 연인 2인 사이의 갈등만. " +
              "(가족·돈·직장 문제가 중심이면 해당 광장으로.)"
   ```

**결론**: **위험도 중간. 캐싱 주의로 관리 가능.**

---

### 옵션 (c): 광장 보강 — FRIEND/WORK Thin Plaza 확대

#### 목표
Phase 7에서 조건 3 미충족의 근본 원인인 FRIEND·WORK 코퍼스 부족(165·156)을 해소.

#### 근거
- QLoRA 임계를 낮추기 위한 전제 조건
- NATEPAN 랭킹 섹션 구조상 한계가 있으나, 추가 채널 활용 가능성 탐색

#### 설계: 멀티채널 콘텐츠 수집

| 채널 | FRIEND 예상 | WORK 예상 | 난이도 | 비고 |
|---|---|---|---|---|
| NATEPAN 랭킹 | ~165 | ~156 | 낮음 | 현황 |
| 블라인드(Blind) | +50~100? | +100~150? | 중간 | 직장인 커뮤니티. crawlers/blind.py 확인 필요 |
| 오늘의유머(Today humor) | +30~50? | — | 높음 | 친구/일상 중심. 새 크롤러 필요 |
| 에브리타임(Everytime) | +30~50? | — | 중간 | 대학생 커뮤니티. 친구/일상. 크롤러 여부 확인 |
| 인스타 계정 시딩 | +? | +? | 높음 | 마케팅 팀 (ASM) 협업 필요 |

#### 실행 절차

1. **크롤러 기존 확인** (읽기만):
   ```bash
   ls /home/justant/Data/Again-Spring/ai-user/learning/app/crawlers/
   # blind.py, dcinside.py, 등 확인
   ```

2. **Blind 크롤러 활용** (가장 가능성 높음):
   - 직장인 커뮤니티 → WORK 카테고리 자료 풍부
   - 예상 +100~150건 추가 가능
   - 기존 크롤러가 있다면 즉시 실행

3. **신규 크롤러 개발** (시간 많이 소요):
   - 오늘의유머(Today humor): 친구 일상 갈등 풍부
   - 에브리타임(Everytime): 대학생 커뮤니티

4. **코퍼스 인제스트 후 임계 재확인**:
   ```
   FRIEND 165 + 50 = 215 → 임계 150 통과
   WORK   156 + 100 = 256 → 임계 200 통과
   ```

#### 코드 위치 (읽기만)

**파일**: `ai-user/learning/app/crawlers/`
- `blind.py` (이미 존재? 확인 필요)
- `dcinside.py` (기존)

**인제스트 프로세스**:
```python
# ai-user/learning/app/api/crawl.py 또는 CLI
# POST /crawl?source=blind&community=work
```

#### 부작용 검토

| 항목 | 위험도 | 완화 |
|---|---|---|
| 크롤링 실패 | 중간 | 소스별 안정성 테스트 후 진행 |
| 데이터 품질 | 높음 | NATEPAN과 다른 문체·구조 → QLoRA 오버피팅 우려 |
| 일정 지연 | **높음** | 신규 크롤러 개발 시 1~2주 소요 |
| 캐싱 영향 | 없음 | 학습 데이터만 변경 |

**결론**: **우선순위 낮음. 옵션 (b)+(a) 먼저 진행 후, FAIL 여전하면 병행.**

---

## 우선순위 권고표

### FAIL 수준별 옵션 선택

| 식별률 | 상황 | 권고 | 근거 |
|---|---|---|---|
| **60~68%** | 경미 | **(b)만** | T3 세부화로 충분. QLoRA 불필요. |
| **68~75%** | 중간 | **(b) → (a)** | (b) 2주 진행 → 재평가. FAIL 계속하면 (a). |
| **75~85%** | 심각 | **(b) + (a)** | 병행 진행. (b) 프롬프트 동시에, (a) WSL 트레이닝. |
| **>85%** | 극심 | **(a) + (c)** | (b)로는 부족. 즉시 QLoRA + 코퍼스 확대. |

### 의사결정 트리

```
Phase 8 결과
├─ 식별률 ≤60% → PASS (프로덕션 배포)
│
├─ 60 < 식별률 ≤68% → (b) 실행
│  └─ 재평가 ≤60% → PASS
│  └─ 재평가 >60% → (b)+(a) 병행
│
├─ 68 < 식별률 ≤75% → (b) + (a) 평행 진행
│  ├─ (b) 2주 소요
│  └─ (a) 동시 WSL 트레이닝
│
└─ 식별률 >75% → (a) 우선 + (c) 병행
   ├─ (a) QLoRA 트레이닝 (4~6시간)
   └─ (c) 코퍼스 확대 (1~2주)
```

---

## 구체적 일정 예시 (옵션 b+a 병행)

### Week 1 (옵션 b)

**월 (2026-06-23)**:
- CATEGORY_GUIDE 세부화 (2시간)
- lint:docs, build 검증 (1시간)

**화 (2026-06-24)**:
- dev 배포 (30분)
- sanity check: 신규 5건 분석 (4시간)

**수~금**:
- 대기 (재평가 준비는 무)

### Week 2 (옵션 a - 동시 진행)

**월 (2026-06-30)**:
- Phase 7 재판정: 임계 A(100건) 결정 (오너)
- WSL에서 SFT 데이터셋 빌드 (2시간)

**화~목**:
- QLoRA 트레이닝 (RTX 3090, 4~6시간)
- 모델 가중치 저장

**금**:
- prod LLM 워커 모델 교체 (30분)
- sanity check: 신규 5건 분석 (2시간)

### Week 3 (Phase 8 재평가)

**월~수**:
- 블라인드 평가 (신선 3인)
- 채점 + 식별률 계산

**목**:
- 최종 판정 (≤60% PASS / >60% 다음 옵션)

---

## 옵션별 Risk-Return 매트릭스

```
Risk
 │
 │  (c) 코퍼스 보강
 │   ●
 │   │     
 │   │     (b) + (a) 병행
 │   │      ●
 │   │      │
 │   │      │     
 │   │      │      (a) QLoRA만
 │   │      │       ●
 │   │      │       │
 └──────────────────●─────────► Return (효과도)
      (b)만      중간    높음
    낮음  중간
```

| 옵션 | Risk | Return | 소요시간 | 비용 |
|---|---|---|---|---|
| (a) QLoRA 임계 | 낮음 | 중간~높음 | 1주 | 낮음 (설정) |
| (b) Phase 6 추가 | 중간 | 높음 | 2주 | 낮음 (프롬프트) |
| (c) 코퍼스 보강 | 높음 | 낮음~중간 | 2~3주 | 높음 (수작업) |
| (b)+(a) 병행 | **중간** | **높음** | **2주** | **낮음** |

**→ (b)+(a) 병행이 Pareto 최적. 시간·비용 대비 식별률 감소 확률 높음.**

---

## 코드 변경 영향도 요약

**설계 단계이므로 코드 미변경. 실행 시 변경 최소화:**

### 옵션 (a) 코드 변경

```
파일:  ai-user/learning/.../persona_strengthener.py 또는 API
변경:  SFT 데이터셋 빌더 threshold 파라미터만
영향:  ZERO (프롬프트/BE 무영향, ML만)
```

### 옵션 (b) 코드 변경

```
파일:  PromptAssembler.java (라인 111-117)
변경:  CATEGORY_GUIDE 문자열 4개만 재작성
영향:  캐싱 깨짐 (1시간, 새벽 배포로 완화)
```

### 옵션 (c) 코드 변경

```
파일:  crawlers/*.py 또는 신규 파일
변경:  크롤러 로직 추가/수정
영향:  높음 (DB 스키마 영향 가능)
```

---

## 최종 권고

### 1순위: **(b) Phase 6 추가 라운드 (T3 광장 세부화)**

**이유**:
- Phase 6이 부분 성공(71% T3 성공)했으므로, 이를 강화하는 것이 자연스러움
- 프롬프트 변경만 → 코드 위험 최소
- 2주 내 재평가 가능

### 2순위: **(a) QLoRA 임계 조정 (동시 병행)**

**이유**:
- (b)를 진행하는 동시에 (a) WSL 트레이닝
- 임계 A(100건)는 달성 가능성 높음
- 최대 효과: (b) + (a) = 60% 이상 감소 확률 높음

### 3순위: **(c) 코퍼스 보강 (보조)**

**이유**:
- (b)+(a) 이후 여전히 FAIL 시만 실행
- Blind 크롤러 즉시 활용 (신규 크롤러 불필요 시)

---

## 부록: 코드 위치 Map (참조용)

| 컴포넌트 | 파일 경로 | 핵심 라인 | 용도 |
|---|---|---|---|
| CATEGORY_GUIDE | PromptAssembler.java | 111-117 | 옵션 (b) |
| assemblePostPrompt | PromptAssembler.java | 152-183 | 옵션 (b) 프롬프트 삽입 |
| VARIETY_SEEDS | PromptAssembler.java | 89-109 | 옵션 (b) 참고 (이미 Phase 6 적용) |
| recentOutputsBlock | PromptAssembler.java | 180 | 옵션 (b) 참고 (이미 Phase 6 적용) |
| SFT 데이터셋 빌더 | persona_strengthener.py | — | 옵션 (a) |
| 광장별 clean count | DB 쿼리 | — | 옵션 (a)·(c) |
| 크롤러 | crawlers/*.py | — | 옵션 (c) |

---

**작성일**: 2026-06-22  
**상태**: 설계만 완료 (코드 변경 0)  
**다음 단계**: Phase 8 결과 확인 후 위 트리에 따라 옵션 실행
