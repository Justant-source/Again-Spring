# ============================================================================
# 다시봄 (Again Spring) Environment Variables
# ============================================================================
# Copy this file to .env and fill in values.
# NEVER commit .env to git — add to .gitignore
#
# Bootstrap only: DB + AS_SECRET_MASTER_KEY.
# JWT/OAuth/mail secrets → MariaDB encrypted_secret (see docs/env/environment-variables.md).

# ============================================================================
# MariaDB
# ============================================================================
# Root password. Used for initial container bootstrap — keep strong in prod.
MARIADB_ROOT_PASSWORD=changeme

# Application database name.
MARIADB_DATABASE=againspring

# Application (non-root) user that the backend connects with.
MARIADB_USER=againspring

# Application user's password.
MARIADB_PASSWORD=changeme

# ============================================================================
# Secret vault (AES-256-GCM)
# ============================================================================
# Master key for encrypted_secret. Example: openssl rand -base64 32
# REQUIRED once vault is seeded — never commit real value.
AS_SECRET_MASTER_KEY=
