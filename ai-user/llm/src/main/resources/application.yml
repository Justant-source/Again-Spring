spring:
  application:
    name: llm-ai-user
  datasource:
    url: ${DB_URL:jdbc:mariadb://localhost:3306/againspring_dev?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC}
    username: ${DB_USER:againspring}
    password: ${DB_PASSWORD:changeme}
    driver-class-name: org.mariadb.jdbc.Driver
    hikari:
      maximum-pool-size: 3
      minimum-idle: 1
      connection-timeout: 10000
      idle-timeout: 600000
      max-lifetime: 1800000

server:
  port: 8092

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

llm:
  worker:
    pool-size: ${LLM_POOL_SIZE:20}
    queue-capacity: ${LLM_QUEUE_CAPACITY:100}
    queue-wait-timeout-ms: ${LLM_QUEUE_WAIT_TIMEOUT_MS:30000}
    default-timeout-ms: ${LLM_DEFAULT_TIMEOUT_MS:120000}
    claude-binary-path: ${CLAUDE_BIN:claude}
    claude-model: ${CLAUDE_MODEL:claude-haiku-4-5-20251001}
  # 글(POST) 전용 모델 오버라이드 — 빈 값이면 claude-model 사용 (문체 현실화 S5)
  post-model: ${LLM_POST_MODEL:}
  api:
    prompt-caching: ${LLM_API_PROMPT_CACHING:true}
    # 캐시 TTL: "5m"(기본, GA — beta 헤더 없음) | "1h"(beta 헤더 — ⚠️ clcocloud에서 Kiro 오라우팅 유발, 직접 API 전용)
    cache-ttl: ${LLM_API_CACHE_TTL:5m}
    # clcocloud Haiku 풀 거절 노드 대응 (2026-06-12): 거절(PROVIDER_ERROR) 한정 재시도 + 소진 시 폴백 모델 1회
    refusal-retries: ${LLM_API_REFUSAL_RETRIES:2}
    refusal-fallback-model: ${LLM_API_REFUSAL_FALLBACK_MODEL:claude-sonnet-4-6}

logging:
  level:
    root: INFO
    com.againspring.aiuser.llm: DEBUG

self-critique:
  enabled: ${SELF_CRITIQUE_ENABLED:false}
  pass-threshold: ${SELF_CRITIQUE_THRESHOLD:5}
  # 추가 AI 상투구 (쉼표 구분 리터럴) — 운영 중 발견 시 재배포 없이 등록
  extra-cliches: ${SELF_CRITIQUE_EXTRA_CLICHES:}

anthropic:
  api-key: ${ANTHROPIC_API_KEY:-}
