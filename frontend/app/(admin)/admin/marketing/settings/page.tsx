'use client';

import { AdminSection } from '@/components/admin/AdminSection';

export default function MarketingSettingsPage() {
  return (
    <AdminSection
      title="마케팅 설정"
      subtitle="마케팅 자동화 운영 설정"
    >
      <div style={{ color: '#888', fontSize: 14, padding: '24px 0' }}>
        설정 항목이 추가될 예정입니다.
      </div>
    </AdminSection>
  );
}
